<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.web.mapper.TopTokenMapper">

    <select id="selectListVO" resultType="com.ruoyi.web.vo.TokenVO">
        select tt.*,
               (select ifnull(_tp.price, 0) from top_token_price _tp where _tp.symbol = tt.symbol) as price
        from top_token tt
        where tt.online = 0
    </select>

</mapper>